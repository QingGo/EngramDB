# Rust / build
/target
/target/
**/target/
Cargo.lock

# External weights (symlinked from external SSD, never committed)
/data/
/data

# Probe / bench outputs
/probes/out*/
*.csv
probe-report*.md
*.tmp

# Python
__pycache__/
*.pyc
.venv/
venv/

# mac
.DS_Store

# logs
*.log
/download.log
!probes/*baseline*.csv
# Local PyO3 build artifact (regenerate with cargo build -p engramdb-pyo3)
python/engramdb/_engramdb.so

# research/: 只入库人写的结论,原始 API payload 可重取(见 research/README.md)
research/*.json
research/d*/
research/upstream/
