# Rust / build
/target
/target/
**/target/
Cargo.lock

# External weights (symlinked from external SSD, never committed)
/data/
/data

# Probe / bench outputs
/probes/out*/
*.csv
probe-report*.md
*.tmp

# Python
__pycache__/
*.pyc
.venv/
venv/

# mac
.DS_Store

# logs
*.log
/download.log
!probes/baseline_*.csv
# Local PyO3 build artifact (regenerate with cargo build -p engramdb-pyo3)
python/engramdb/_engramdb.so
