[package]
name = "engramdb-keygen"
version = "0.2.10"
edition = "2021"
license = "Apache-2.0"
description = "Qwen PLE / DeepSeek Engram n-gram deterministic key generation (rowid) in Rust"
repository = "https://github.com/QingGo/EngramDB"

[dependencies]

[dev-dependencies]
serde_json = "1"
