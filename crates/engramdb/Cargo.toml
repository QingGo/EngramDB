[package]
name = "engramdb"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Disk-first storage engine for Engram/PLE n-gram memory tables: build/index/warm/verify/inspect"
categories = ["database", "filesystem", "performance"]
keywords = ["engram", "ple", "ngram", "disk", "embedding"]
readme = "../../README.md"

[dependencies]
engramdb-core = { path = "../engramdb-core", version = "0.1.0" }
engramdb-io = { path = "../engramdb-io", version = "0.1.0" }
engramdb-keygen = { path = "../engramdb-keygen", version = "0.1.0" }
serde_json = "1"
