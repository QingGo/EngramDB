[package]
name = "engramdb"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Disk-first storage engine for Engram/PLE n-gram memory tables: build/index/warm/verify/inspect"
categories = ["database", "filesystem", "performance"]
keywords = ["engram", "ple", "ngram", "disk", "embedding", "memory-table"]
readme = "../../README.md"

[dependencies]
engramdb-core = { path = "../engramdb-core" }
engramdb-io = { path = "../engramdb-io" }
engramdb-keygen = { path = "../engramdb-keygen" }
serde_json = "1"
