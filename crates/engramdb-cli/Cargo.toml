[package]
name = "engramdb-cli"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "EngramDB CLI: build/index/warm/verify/inspect"

[dependencies]
engramdb-core = { path = "../engramdb-core" }
engramdb-io = { path = "../engramdb-io" }
engramdb-keygen = { path = "../engramdb-keygen" }
serde_json = "1"
