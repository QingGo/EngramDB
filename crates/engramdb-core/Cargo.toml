[package]
name = "engramdb-core"
version = "0.1.3"
edition = "2021"
license = "Apache-2.0"
description = "EngramDB core: badge layout, direct-addressed row store, batch gather primitives (no IO backend)"
repository = "https://github.com/QingGo/EngramDB"

[dependencies]
